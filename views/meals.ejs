<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@700&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="styles/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>MealsZZ</title>
</head>
<body id="background">
    <div class="container">

        <!-- Navbar from Bootstrap (customized) -->
        <header class="justify-content-center border-bottom text-center my-3 ">
         <a href="/" class="link-body-emphasis text-decoration-none">
            
            <span class="fs-4"><h1 style="color: #008000; font-weight: bolder;">MealZZ</h1></span>
         </a>
        </header>
    </div>

    <!-- Meal Title puuled from the backend -->
    <div class="Meal-Title">
        <h2> Explore our <strong><%= mealType %></strong> Meals Below!</h2>
        <p>Maybe learn a recipe or two</p>
    </div>

    <!-- All Meal display information pulled from the backed (Meal-image & Meal-Title)-->
    <div class="Meals">
        <!-- For loop goes through all meals in a category chosen by the user and displays each meal using a bootstrap card -->
        <% for(var i = 0; i < meals.length ; i++){ %>
            <div class="card" style="width: 18rem;">
                <img src= <%= meals[i].strMealThumb %> class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title"><%= meals[i].strMeal %></h5>
                    <!-- Input to view Meal recipe -->
                    <form id="meal-p" method="post">
                        <input id="post" type="hidden" name="meal-ID" value=<%= meals[i].idMeal %>>
                        <input id="post" class="btn btn-success" type="submit" value="Check this out" formaction="/recipe">
                    </form>
                </div>
            </div>
        <% } %>
    </div>
    
</body>
</html>